<% if @user.errors.any? %>
  <h2>
    The following errors prevented the user from getting created.  
  </h2>
  <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<h1>
  Edit dis beeb
</h1>

<%= render 'users/form' %>